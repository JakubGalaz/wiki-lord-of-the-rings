import { ChangeRiceColorPipe } from './change-rice-color.pipe';

describe('ChangeRiceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeRiceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
